# config/stt_corrections.yaml — раунд 27 (E1): словарь типичных ошибок
# распознавания Whisper.
#
# Формат: "как Whisper услышал" -> "что имелось в виду". Применяется
# СРАЗУ после распознавания, до intent_router/LLM - заменяется ПО ГРАНИЦЕ
# СЛОВА, без учёта регистра (не голая подстрока).
#
# Как пополнять: смотри logs/gaps.jsonl (раунд 27, gap_log.py) - там
# накапливаются случаи, где Джарвис не понял/не смог что-то сделать;
# если причина в неправильно расслышанном слове - добавляй сюда строку.
# Файл можно редактировать прямо на ходу, сервер перечитывает его сам,
# перезапуск не нужен.

темножить: умножить
